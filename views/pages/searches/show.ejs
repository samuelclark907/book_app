<!DOCTYPE html>
<html>
<head>
  <title></title>

</head>
<body>
  <main>
    <div>
      <% bikes.forEach( book => { %>
      <section>
        <img src="<%= book.image %>">
        <h3><%= book.title %></h3>
        <h3><%= book.author %></h3>
        <p><%= book.description %></p>
      </section>
      <% }) %>
    </div>
  </main>

</body>
</html>